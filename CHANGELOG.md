# Changelog

本文件记录 CPA Monitor Bar 的用户可见变化，版本号遵循语义化版本。

## [Unreleased]

## [1.2.0] - 2026-08-05

### 新增

- 支持为每个 Keeper 供应商独立配置 Sub2API 地址和监控 Key，并在供应商页展示钱包余额、订阅额度或 API Key 限额。
- 支持在设置中验证供应商用量连接；未配置或未启用的供应商不会访问外部用量接口。

### 优化

- 管理员密码与供应商监控 Key 统一保存到 `~/.cpamonitorbar/config.toml`，按 Keeper 地址和供应商 ID 隔离。
- 设置表单滚动或重新打开时主动释放密码输入焦点，应用期间锁定提交快照，避免混用编辑前后的配置。
- 清理旧 Keychain、中间态供应商类型、重复验证监听和未使用的状态字段。

### 修复

- 修复供应商用量重叠刷新时旧请求可能覆盖新结果或清空新任务句柄的问题。
- 修复供应商验证结果可能被旧异步请求覆盖，以及修改 Key 后仍显示旧成功状态的问题。
- 修复 Keeper 已删除或停用供应商后，本地孤立配置与监控 Key 无法清理的问题。
- 修复异常空供应商响应、列表尚未加载或列表变化时可能误删本地监控 Key 的问题。
- 修复供应商配置损坏或批量 Key 更新失败时可能产生部分写入和配置不一致的问题。

### 安全

- 本地凭证目录和文件分别限制为 `0700` 与 `0600`，并拒绝符号链接、硬链接、错误所有者、过宽权限和超大配置文件。
- 凭证更新采用临时文件、原子替换和目录同步；多供应商 Key 通过单次批量事务更新。
- Sub2API 查询使用独立临时会话，禁用 Cookie 与缓存、拒绝重定向，并限制超时和响应体大小。

### 升级提示

- 本版本不读取或迁移旧版 Keychain 凭证；升级后请在设置中重新输入 Keeper 管理员密码，并为需要余额监控的供应商配置 Sub2API Key。

## [1.1.3] - 2026-07-29

### 新增

- 在概览页增加固定 24 小时的请求健康热力图，并展示成功率及成功、失败请求数量。

### 优化

- 将使用概览、请求健康和 Token 占比统一为内嵌标题与细色标卡片，增强区域层次并保持紧凑布局。
- 弱化请求健康标题信息，移除重复的颜色图例，继续让 Token 占比列表独立滚动。

## [1.1.2] - 2026-07-24

### 优化

- 调整供应商趋势的健康状态配色与柱高，降低短暂波动的视觉严重程度。

### 修复

- 兼容 Keeper 1.13.7 将概览时间序列改为 `buckets` 与数值数组后的响应格式。

## [1.1.1] - 2026-07-23

### 修复

- 修复从菜单栏面板打开设置时，设置窗口没有激活并前置的问题。
- 兼容 Keeper 1.13.6 移除 `service_health` 后，概览成功率无法展示的问题。

## [1.1.0] - 2026-07-22

### 新增

- 支持将同一个无边框监控面板原地钉住，点击其他应用时继续保持展示。
- 钉住与取消钉住不会重建窗口或页面；置顶状态不会跨应用重启保存。
- 钉住后的面板可在所在显示器的各 macOS 桌面及全屏 Space 中继续展示。
- 认证文件页支持按当前分类批量刷新额度，并跟踪异步任务直至缓存更新。
- 支持在设置中配置无默认值的全局快捷键，用于在鼠标附近显示或收起监控窗口。

### 优化

- 移除监控面板顶部指向尖头，失焦时保持原有配色。
- 拖动改用 macOS 原生窗口拖动，减少闪动并准确跟随鼠标。
- 全局快捷键唤出面板时改为跟随鼠标所在显示器和位置，并自动避让屏幕边缘。
- 移除没有界面消费者的实时概览请求及对应模型，减少每轮刷新开销。

### 修复

- 修复状态栏坐标尚未就绪时，监控面板显示在屏幕左下角而难以发现的问题。
- 将 Keychain 凭证读写移出主线程，避免新安装后冷启动期间状态栏点击无响应。
- 监控面板改为非激活 Panel，修复菜单栏应用冷启动时点击图标后窗口被立即隐藏的问题。

### 安全

- 仅放行额度刷新及其单任务状态查询；额度重置、巡检和认证文件管理仍被拒绝。
- 远程 HTTP 连接增加显式风险确认，并拒绝所有 HTTP 重定向。
- 登录 Cookie 改为仅在登录成功响应中手动接收。
- 响应体、列表数量和额度刷新任务增加客户端安全上限。
- 切换 Keeper 时尽力注销旧会话，新地址登录成功后清理旧地址密码。

## [1.0.0] - 2026-07-21

### 新增

- 面向 `cpa-usage-keeper` 的 macOS 26.5+ 菜单栏监控窗口。
- 使用概览、总成本及模型、API Key、认证文件、AI 提供商 Token 占比。
- 请求事件明细、SSE / WS 类型、延迟合并展示和滚动分页加载。
- 启用认证账号的限额缓存与重置时间展示。
- 供应商最近 5 小时健康趋势。
- 最近 8 小时、今天、昨天时间范围与可配置刷新频率。
- Keychain 管理员密码、启动自动恢复登录和退出应用前注销会话。
- 开机自动启动、Keeper 版本显示和浏览器快捷入口。
- HTTP / HTTPS 地址支持及 HTTP 未加密风险提示。
- Universal 2 ad-hoc 测试 DMG、应用图标和安装布局。

### 安全

- 使用固定请求 Method 与路径白名单。
- 限额功能仅读取 Keeper 缓存，不触发刷新、重置或巡检。
- 打包时验证签名、rpath、版本、架构，并扫描常见凭据标记。
